# Environment variables for React app
# Note: Only variables starting with REACT_APP_ are accessible in the app

# API URL - Update this with your Render backend URL for production
REACT_APP_API_URL=http://localhost:5000/api
